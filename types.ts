
export interface StyleOption {
  id: string;
  label: string;
}

export interface ResolutionOption {
  id: string;
  label: string;
}

export interface AspectRatioOption {
  id: string;
  label: string;
}
